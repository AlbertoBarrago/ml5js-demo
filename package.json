{
  "name": "devai",
  "author": "alBz",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "start": "bun server.js",
    "build": "rm -rf dist && mkdir -p dist && cp -r src/* dist/",
    "postbuild": "surge dist hard-language.surge.sh"
  }
}
